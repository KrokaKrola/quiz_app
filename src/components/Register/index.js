import React from 'react';

const Register = () => {
  return (
    <>
      <form className="Register">
        <input type="text" placeholder="Enter your name" />
        <button type="submit">Register and choose intrested category</button>
      </form>
    </>
  );
};

export default Register;